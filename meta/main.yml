galaxy_info:
  author: Mahdad Ghasemian
  description: Ansible galaxy role can run a postgresql on a docker container
  license: "license (BSD, MIT)"
  min_ansible_version: 2.1
  platforms:
    - name: Ubuntu
      versions:
        - all
  galaxy_tags:
    - docker
    - container
    - postgresql

dependencies: []